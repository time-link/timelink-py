### Migration with alembic

To generate new migration evoke alembic at the command line in the
timelink-py base directory.

Migrations are generated targetted at the reference database
defined by `reference_database` variable in alembic.ini


#### Auto migration

Generate migration to match the current models

```sh
alembic revision --autogenerate -m "{migration_message}"
```